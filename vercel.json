{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter=@vms/shared run build && pnpm --filter=@vms/web run build && mkdir -p dist && cp -a apps/web/dist/. dist/",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
